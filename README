1) install git
2) install tmux
3) install vim
4) install ack
5) git clone https://toranb@bitbucket.org/toranb/super-basic-dotfilez.git dotfilez
6) cd dotfilez
7) bash symlink.sh
8) restart terminal

**if you want to use the python functions install jedi globally

sudo pip install jedi
